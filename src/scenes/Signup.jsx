export default function Signup ({setUser}) {

    return (
        <>
        <section style={{background: "#ff000030"}}>
        <h1>Sign Up</h1>
        <button onClick={() => setUser('Rturning User')}>Sign Up</button>

        </section>
        
        </>
    )
}